export type ProjectListItem = {
  id: string;
  name: string;
  slug: string;
  updatedAt: string;
};

export type ProjectDetail = {
  id: string;
  name: string;
  slug: string;
  createdAt: string;
  updatedAt: string;
};
