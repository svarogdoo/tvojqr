# INTENT

## User Request

The backend build passes on Railway, but deploy fails. Fix the deployment setup.

## Requested Outcome

Make the backend deploy reliably on Railway with the current repo structure.

## Constraints

- Keep the fix minimal and focused on deployment/runtime setup.
- Verification should use repo-level reasoning plus build checks.

## Open Questions

- Exact Railway runtime log is not yet provided.
