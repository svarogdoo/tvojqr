# INTENT

## User Request

The backend now builds on Railway but fails at runtime/deploy completion. Fix the likely runtime configuration issue.

## Requested Outcome

Improve Railway runtime compatibility and identify the remaining deploy blocker.

## Constraints

- Keep the fix minimal and production-safe.
- Exact Railway runtime log is still needed to confirm the final cause.

## Open Questions

- What is the exact runtime error line from Railway?
