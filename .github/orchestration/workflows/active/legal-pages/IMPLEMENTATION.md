# Implementation

1. Create `/terms` with Svarog d.o.o. identity and SaaS protections.
2. Create `/privacy` with GDPR-oriented data handling language.
3. Add footer links.
4. Run `npm run check` and `npm run build` in `frontend/`.
